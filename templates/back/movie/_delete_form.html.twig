<form class="mt-3" method="post" action="{{ path('app_back_movie_delete', {'id': movie.id}) }}" onsubmit="return confirm('êtes-vous sûr de vouloir supprimer ce film?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ movie.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
