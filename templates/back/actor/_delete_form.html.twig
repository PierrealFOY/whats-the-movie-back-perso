<div class="mt-3 mb-3">
    <form method="post" action="{{ path('app_back_actor_delete', {'id': actor.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ actor.id) }}">
        <button class="btn btn-danger">Supprimer</button>
    </form>
</div>
