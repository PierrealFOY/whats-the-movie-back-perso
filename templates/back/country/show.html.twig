{% extends 'base.html.twig' %}

{% block title %}Actor{% endblock %}

{% block body %}
    <h1>Acteur</h1>

    <div class="mb-3">
        <a href="{{ path('app_back_country_index') }}" >Retour à la liste des pays</a>
    </div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ country.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ country.name }}</td>
            </tr>
        </tbody>
    </table>

    

    <a href="{{ path('app_back_country_edit', {'id': country.id}) }}" class="btn btn-warning">Modifier</a>

    {{ include('back/country/_delete_form.html.twig') }}
{% endblock %}
