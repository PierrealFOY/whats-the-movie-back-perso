<div class="mt-3 mb-3">
    <form method="post" action="{{ path('app_back_country_delete', {'id': country.id}) }}" onsubmit="return confirm('êtes-vous sûr de vouloir supprimer ce pays?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ country.id) }}">
        <button class="btn btn-warning">Supprimer</button>
    </form>
</div>
