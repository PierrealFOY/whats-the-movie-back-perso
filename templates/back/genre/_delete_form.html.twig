<div class="mt-3 mb-3">
    <form method="post" action="{{ path('app_back_genre_delete', {'id': genre.id}) }}" onsubmit="return confirm('êtes-vous sûr de vouloir supprimer cette genre?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ genre.id) }}">
        <button class="btn btn-warning">Supprimer</button>
    </form>
</div>
