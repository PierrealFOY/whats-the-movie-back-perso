{% extends 'back/user/layout/layout.html.twig' %}

{% block title %}User{% endblock %}

{% block subBody %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Names</th>
                <td>{{ user.name }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Mot de Passe</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Score</th>
                <td>{{ user.score }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ user.picture }}</td>
            </tr>
        </tbody>
    </table>

    <div>
        <a href="{{ path('app_back_user_list') }}">Retourner à la liste des utilisateurs</a>
    </div>
    <div>
        <a href="{{ path('app_back_user_edit', {'id': user.id}) }}">Editer</a>
    </div>

   {# {{ include('back/user/_delete_form.html.twig') }} #}
{% endblock %}